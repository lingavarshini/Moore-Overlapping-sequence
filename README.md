# Moore-Overlapping-sequence
A Moore Overlapping Sequence Detector is a Finite State Machine (FSM) where the output depends only on the current state, not directly on the input. In this design, the FSM detects a specific binary sequence from a serial input stream while supporting overlapping, meaning part of a previously detected sequence can be reused to detect the next one.
